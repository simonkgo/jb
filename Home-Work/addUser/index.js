const userModule = require('./register');

const login = require('./login')


    const user = {name:'Moishe',password:123};
    
    
        userModule.add(user);
        login.verify(user)
       
    
